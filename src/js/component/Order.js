import React from "react";

const Order = () => {
	return (
		<div className="order">
			<h1>Buttons</h1>
			<button>Wave ended</button>
			<br />
			<button>login</button>
		</div>
	);
};

export default Order;
